//this will fail because of asset imports. you can set this up
// import { babylonInit } from "app";
// import { expect } from "chai";

// describe("App works!", () => {
//     // Test to confirm that tests work.

//     it("should not crash", () => {
//         return babylonInit()
//             .then(() => {
//                 expect(true).to.equal(true);
//                 //scene started rendering, everything is initialized
//                 //success = true;
//             })
//             .catch((e) => {
//                 expect(e).to.equal(null)
//                 //success = false;
//             });

//         //expect(App).to.equal(true);
//     });
// });

